import processing.core.PImage;

import java.util.List;

public class Gem extends AnimationEntity {

    public Gem(String id, Point position, List<PImage> images, double animationPeriod) {
        super(id, position, images, animationPeriod);
    }
}
